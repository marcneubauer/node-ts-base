"use strict";
const expect = require('chai').expect;
describe('class or domain', () => {
    describe('unit', () => {
        it('should be true', () => {
            // const objectiveness = typeof(this) == 'object';
            const objectiveness = true;
            expect(objectiveness).to.be.true;
        });
    });
});
//# sourceMappingURL=index.test.js.map